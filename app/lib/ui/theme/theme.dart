/// The app shell: design tokens, themes, formatting helpers, the shared
/// widgets every screen is built from, and the provider seam that injects the
/// platform layer.
///
/// One import gets a screen everything it needs from this layer:
///
/// ```dart
/// import 'package:ledger/ui/theme/theme.dart';
/// ```
library;

export 'app_dependencies.dart';
export 'app_theme.dart';
export 'formatting.dart';
export 'palette.dart';
export 'tokens.dart';
export 'widgets.dart';
